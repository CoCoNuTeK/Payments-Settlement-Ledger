---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "blazor-service"
  labels:
    app.kubernetes.io/name: "PaymentsLedger"
    app.kubernetes.io/component: "blazor"
    app.kubernetes.io/instance: "{{.Release.Name}}"
spec:
  type: "ClusterIP"
  selector:
    app.kubernetes.io/name: "PaymentsLedger"
    app.kubernetes.io/component: "blazor"
    app.kubernetes.io/instance: "{{.Release.Name}}"
  ports:
    - name: "http"
      protocol: "TCP"
      port: "{{ .Values.parameters.blazor.port_http }}"
      targetPort: "{{ .Values.parameters.blazor.port_http }}"
    - name: "https"
      protocol: "TCP"
      port: "{{ .Values.parameters.blazor.port_https }}"
      targetPort: "{{ .Values.parameters.blazor.port_https }}"
