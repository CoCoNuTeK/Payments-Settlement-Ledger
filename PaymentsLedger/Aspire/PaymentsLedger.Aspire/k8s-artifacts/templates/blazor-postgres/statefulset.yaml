---
apiVersion: "apps/v1"
kind: "StatefulSet"
metadata:
  name: "blazor-postgres-statefulset"
  labels:
    app.kubernetes.io/name: "PaymentsLedger"
    app.kubernetes.io/component: "blazor-postgres"
    app.kubernetes.io/instance: "{{.Release.Name}}"
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "PaymentsLedger"
        app.kubernetes.io/component: "blazor-postgres"
        app.kubernetes.io/instance: "{{.Release.Name}}"
    spec:
      containers:
        - image: "docker.io/library/postgres:17.6"
          name: "blazor-postgres"
          envFrom:
            - configMapRef:
                name: "blazor-postgres-config"
            - secretRef:
                name: "blazor-postgres-secrets"
          ports:
            - name: "tcp"
              protocol: "TCP"
              containerPort: 5432
          imagePullPolicy: "IfNotPresent"
  selector:
    matchLabels:
      app.kubernetes.io/name: "PaymentsLedger"
      app.kubernetes.io/component: "blazor-postgres"
      app.kubernetes.io/instance: "{{.Release.Name}}"
  replicas: 1
  persistentVolumeClaimRetentionPolicy: {}
  updateStrategy:
    rollingUpdate:
      maxUnavailable: 0
    type: "RollingUpdate"
